
# Vchat 社交聊天系统 - 业务逻辑文档

## 一、项目概述

Vchat 是一个基于 ThinkPHP 5.0 框架开发的社交聊天系统，主要功能包括：
- 用户注册与登录
- 实时点对点聊天
- 动态发布与互动（评论、点赞、收藏）
- 好友管理
- 后台管理系统

---

## 二、核心技术栈

- **后端框架**：ThinkPHP 5.0
- **实时通讯**：Workerman + Socket.IO
- **前端框架**：Bootstrap + jQuery
- **数据库**：MySQL

---

## 三、数据库设计

### 3.1 数据表概览

| 表名 | 前缀 | 说明 |
|------|------|------|
| ch_user | ch_ | 用户表 |
| ch_admin | ch_ | 管理员表 |
| ch_mood | ch_ | 动态表 |
| ch_comment | ch_ | 评论表 |
| ch_favorite | ch_ | 点赞表 |
| ch_collect | ch_ | 收藏表 |
| ch_friend | ch_ | 好友表 |
| ch_link | ch_ | 链接表 |
| ch_sysinfo | ch_ | 系统信息表 |
| ch_log | ch_ | 日志表 |

### 3.2 详细数据表结构

#### 用户表 (ch_user)
| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | int(10) unsigned | 主键 |
| name | varchar(50) | 用户名 |
| password | char(35) | 密码（MD5加密） |
| sex | tinyint(4) | 性别（0未知，1男，2女） |
| age | int(10) unsigned | 年龄 |
| icon | varchar(255) | 头像路径 |
| status | tinyint(4) | 状态（0禁用，1正常） |
| identity | tinyint(4) | 身份（1学生，2老师） |
| department | varchar(20) | 院系 |
| operator | varchar(50) | 操作人 |
| create_time | int(11) | 创建时间 |
| modified_time | int(11) | 修改时间 |

#### 管理员表 (ch_admin)
| 字段 | 类型 | 说明 |
|------|------|------|
| admin_id | int(11) | 主键 |
| name | varchar(50) | 用户名 |
| password | char(35) | 密码 |
| operator | varchar(50) | 操作人 |
| create_time | int(11) | 创建时间 |
| modified_time | int(11) | 修改时间 |

#### 动态表 (ch_mood)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int(10) unsigned | 主键 |
| uid | int(11) | 发布用户ID |
| content | text | 动态内容 |
| createtime | int(11) | 创建时间 |
| display | tinyint(4) | 是否显示（0不显示，1显示） |

#### 评论表 (ch_comment)
| 字段 | 类型 | 说明 |
|------|------|------|
| comment_id | int(10) unsigned | 主键 |
| user_id | int(11) | 评论用户ID |
| mood_id | int(11) | 动态ID |
| content | varchar(500) | 评论内容 |
| display | tinyint(1) | 是否显示 |
| create_time | int(11) | 创建时间 |

#### 点赞表 (ch_favorite)
| 字段 | 类型 | 说明 |
|------|------|------|
| favorite_id | int(10) unsigned | 主键 |
| user_id | int(11) | 用户ID |
| mood_id | int(11) | 动态ID |
| display | tinyint(1) | 是否显示 |
| create_time | int(11) | 创建时间 |

#### 收藏表 (ch_collect)
| 字段 | 类型 | 说明 |
|------|------|------|
| collect_id | int(10) unsigned | 主键 |
| user_id | int(11) | 用户ID |
| mood_id | int(11) | 动态ID |
| display | tinyint(1) | 是否显示 |
| create_time | int(11) | 创建时间 |

#### 好友表 (ch_friend)
| 字段 | 类型 | 说明 |
|------|------|------|
| friend_id | int(10) unsigned | 主键 |
| uid | int(10) unsigned | 用户ID |
| fid | int(10) unsigned | 好友用户ID |
| status | tinyint(4) | 状态（1申请中，2已通过） |
| create_time | int(11) | 创建时间 |

---

## 四、业务流程详解

### 4.1 用户模块

#### 4.1.1 用户注册流程
1. 用户访问注册页面，填写注册信息（用户名、密码、性别、年龄、院系、身份）
2. 提交表单到 `User/DoRegister`
3. 系统检查用户名是否已存在
4. 密码使用 MD5 加密
5. 创建用户记录，同时自动添加自己为好友（双向关系）
6. 跳转至登录页面

**相关文件**：
- [application/home/controller/User.php](file:///workspace/application/home/controller/User.php#L98-L120)
- [application/common/Service/User.php](file:///workspace/application/common/Service/User.php)

#### 4.1.2 用户登录流程
1. 用户访问登录页面
2. 填写用户名、密码，选择身份
3. 提交表单到 `User/Login`
4. 系统验证用户名、密码是否正确
5. 验证身份是否匹配
6. 检查用户状态是否正常（未被禁用）
7. 保存用户信息到 Session，跳转至首页

**相关文件**：
- [application/home/controller/User.php](file:///workspace/application/home/controller/User.php#L8-L38)

#### 4.1.3 用户信息编辑流程
1. 用户在个人中心编辑基本信息或修改密码
2. 提交表单到 `User/Edit`
3. 验证原始密码（修改密码时）
4. 更新用户信息
5. 保存新用户信息到 Session

**相关文件**：
- [application/home/controller/User.php](file:///workspace/application/home/controller/User.php#L52-L94)

#### 4.1.4 头像上传流程
1. 用户在个人中心选择图片文件
2. 提交表单到 `User/EditIcon`
3. 上传文件到服务器
4. 更新用户头像路径
5. 更新 Session 中的用户信息

**相关文件**：
- [application/home/controller/User.php](file:///workspace/application/home/controller/User.php#L145-L170)

### 4.2 好友模块

#### 4.2.1 发送好友申请流程
1. 用户点击添加好友按钮
2. 发起 AJAX 请求到 `Friend/ajaxAddFriend`
3. 系统检查是否已发送过申请或已是好友
4. 如未申请，创建好友记录，status=1（申请中）
5. 返回状态码给前端

**返回状态码**：
- 0：失败
- 1：成功
- 2：已申请
- 3：不能添加自己

**相关文件**：
- [application/home/controller/Friend.php](file:///workspace/application/home/controller/Friend.php#L14-L38)

#### 4.2.2 接受好友申请流程
1. 用户在个人中心查看好友申请列表
2. 点击接受按钮，请求到 `Friend/AddFriend`
3. 将申请记录的 status 更新为 2（已通过）
4. 创建反向好友记录（双方互加）
5. 跳转回个人中心

**相关文件**：
- [application/home/controller/Friend.php](file:///workspace/application/home/controller/Friend.php#L70-L96)

#### 4.2.3 拒绝好友申请流程
1. 用户点击拒绝按钮
2. 请求到 `Friend/RefuseFriend`
3. 删除好友申请记录
4. 跳转回好友申请列表

**相关文件**：
- [application/home/controller/Friend.php](file:///workspace/application/home/controller/Friend.php#L133-L145)

#### 4.2.4 删除好友流程
1. 用户在好友列表点击删除
2. 请求到 `Friend/DeleteFriend`
3. 删除双向好友关系记录
4. 返回成功

**相关文件**：
- [application/home/controller/Friend.php](file:///workspace/application/home/controller/Friend.php#L116-L131)

#### 4.2.5 好友列表流程
1. 用户访问个人中心，进入好友列表
2. Center/index 加载好友列表页面
3. 根据用户ID查询状态为2的好友关系
4. 根据好友ID查询用户信息
5. 渲染到页面

**相关文件**：
- [application/home/controller/Center.php](file:///workspace/application/home/controller/Center.php)

### 4.3 聊天模块

#### 4.3.1 聊天初始化流程
1. 用户登录后，Socket.IO 客户端连接服务器（端口2120）
2. 客户端发送 login 事件，携带用户ID
3. 服务器将用户加入在线用户列表
4. 服务器向客户端推送当前在线用户列表

**相关文件**：
- [start_io.php](file:///workspace/start_io.php#L20-L44)

#### 4.3.2 发送消息流程
1. 用户在聊天窗口输入消息
2. 选择好友并发送
3. 前端通过 Socket.IO 发送 send_msg 事件
4. 服务器解析消息，获取发送者ID、接收者ID和消息内容
5. 服务器向接收者发送 new_msg 事件
6. 接收者收到消息并显示

**相关文件**：
- [start_io.php](file:///workspace/start_io.php#L65-L82)

#### 4.3.3 聊天页面加载流程
1. 用户点击好友头像进入聊天
2. 请求到 Chat/index，携带好友ID
3. 查询好友信息
4. 加载聊天页面模板

**相关文件**：
- [application/home/controller/Chat.php](file:///workspace/application/home/controller/Chat.php)

### 4.4 动态模块

#### 4.4.1 发布动态流程
1. 用户在首页或个人中心输入动态内容
2. 提交表单到 Mood/AddMood
3. 保存动态信息（内容、用户ID、创建时间、display=1）
4. 跳转回首页

**相关文件**：
- [application/home/controller/Mood.php](file:///workspace/application/home/controller/Mood.php#L10-L23)

#### 4.4.2 查看动态流程
1. 用户访问首页
2. Index/index 查询所有 display=1 的动态
3. 查询每条动态的评论信息
4. 查询评论用户的信息
5. 渲染到页面

**相关文件**：
- [application/home/controller/Index.php](file:///workspace/application/home/controller/Index.php)

#### 4.4.3 删除动态流程
1. 用户在自己的动态列表点击删除
2. 请求到 Mood/DeleteMood，携带动态ID
3. 删除动态记录
4. 级联删除该动态的点赞、评论、收藏记录
5. 跳转回个人中心

**相关文件**：
- [application/home/controller/Mood.php](file:///workspace/application/home/controller/Mood.php#L25-L43)

### 4.5 互动模块

#### 4.5.1 评论动态流程
1. 用户在动态下方输入评论内容
2. 提交表单到 Comment/AddComment
3. 保存评论信息（内容、用户ID、动态ID、创建时间）
4. 跳转回首页

**相关文件**：
- [application/home/controller/Comment.php](file:///workspace/application/home/controller/Comment.php)

#### 4.5.2 点赞动态流程
1. 用户点击动态的点赞按钮
2. 前端发起 AJAX 请求到 Favorite/ajaxChangeFavorite，status=1
3. 创建点赞记录，display=1
4. 返回状态码
5. 前端更新显示

**相关文件**：
- [application/home/controller/Favorite.php](file:///workspace/application/home/controller/Favorite.php#L13-L36)

#### 4.5.3 取消点赞流程
1. 用户再次点击已点赞的动态
2. 前端发起 AJAX 请求到 Favorite/ajaxChangeFavorite，status=0
3. 更新点赞记录，display=0
4. 返回状态码
5. 前端更新显示

**相关文件**：
- [application/home/controller/Favorite.php](file:///workspace/application/home/controller/Favorite.php#L38-L51)

#### 4.5.4 收藏动态流程
1. 用户点击动态的收藏按钮
2. 前端发起 AJAX 请求到 Collect/ajaxChangeCollect，status=1
3. 创建收藏记录，display=1
4. 返回状态码
5. 前端更新显示

**相关文件**：
- [application/home/controller/Collect.php](file:///workspace/application/home/controller/Collect.php#L13-L36)

#### 4.5.5 取消收藏流程
1. 用户再次点击已收藏的动态
2. 前端发起 AJAX 请求到 Collect/ajaxChangeCollect，status=0
3. 更新收藏记录，display=0
4. 返回状态码
5. 前端更新显示

### 4.6 后台管理模块

#### 4.6.1 管理员登录流程
1. 访问后台登录页面
2. 输入管理员用户名和密码
3. 验证通过后保存管理员信息到 Session
4. 跳转至后台首页

**相关文件**：
- [application/admin/controller/Login.php](file:///workspace/application/admin/controller/Login.php)

#### 4.6.2 用户管理流程
1. 管理员访问用户管理页面
2. 查询用户列表（支持分页）
3. 支持编辑用户信息、设置用户状态（启用/禁用）、修改密码
4. 支持新增用户

**相关文件**：
- [application/admin/controller/User.php](file:///workspace/application/admin/controller/User.php)

#### 4.6.3 动态管理流程
- 管理员可以查看所有动态
- 可以删除不当动态
- 可以隐藏动态（设置 display=0）

**相关文件**：
- [application/admin/controller/Mood.php](file:///workspace/application/admin/controller/Mood.php)

#### 4.6.4 评论管理流程
- 管理员可以查看所有评论
- 可以删除不当评论

**相关文件**：
- [application/admin/controller/Comment.php](file:///workspace/application/admin/controller/Comment.php)

#### 4.6.5 系统信息管理流程
- 管理员可以修改系统名称
- 可以上传系统 Logo

**相关文件**：
- [application/admin/controller/Sysinfo.php](file:///workspace/application/admin/controller/Sysinfo.php)

---

## 五、系统架构

### 5.1 目录结构

```
/workspace
├── application/            # 应用目录
│   ├── admin/             # 后台模块
│   │   ├── controller/    # 后台控制器
│   │   └── view/          # 后台视图
│   ├── home/              # 前台模块
│   │   ├── controller/    # 前台控制器
│   │   └── view/          # 前台视图
│   ├── common/            # 公共模块
│   │   ├── Service/       # 业务服务层
│   │   ├── model/         # 模型层
│   │   ├── controller/    # 公共控制器
│   │   └── common/        # 公共函数
│   ├── config.php         # 应用配置
│   ├── database.php       # 数据库配置
│   └── route.php          # 路由配置
├── public/                # WEB 目录
│   ├── static/            # 静态资源
│   │   ├── Admin/         # 后台静态资源
│   │   ├── Home/          # 前台静态资源
│   │   └── Chat/          # 聊天模块资源
│   └── index.php          # 入口文件
├── Uploads/               # 上传文件目录
├── vendor/                # Composer 依赖
├── think                  # CLI 工具
├── start_web.php          # Web 服务启动脚本
├── start_io.php           # Socket.IO 服务启动脚本
└── ch_chat.sql            # 数据库文件
```

### 5.2 MVC 架构

系统采用经典的 MVC 架构：
- **Model（模型层）**：位于 `application/common/model/`，负责数据操作
- **View（视图层）**：位于 `application/{module}/view/`，负责页面展示
- **Controller（控制器层）**：位于 `application/{module}/controller/`，负责业务逻辑控制

此外，还有 **Service 层**（业务服务层）位于 `application/common/Service/`，封装了核心业务逻辑，供控制器调用。

### 5.3 实时通讯架构

- Socket.IO 服务启动在 2120 端口
- 内部 HTTP 接口服务启动在 2121 端口
- 使用 Workerman 作为底层服务框架
- 支持在线状态实时更新
- 支持消息实时推送

---

## 六、关键文件说明

### 6.1 核心入口文件

| 文件 | 说明 |
|------|------|
| public/index.php | Web 应用入口 |
| start_web.php | Web 服务启动脚本 |
| start_io.php | Socket.IO 服务启动脚本 |
| think | CLI 命令行工具 |

### 6.2 配置文件

| 文件 | 说明 |
|------|------|
| application/config.php | 应用主配置 |
| application/database.php | 数据库配置 |
| application/route.php | 路由配置 |

### 6.3 前台关键控制器

| 控制器 | 说明 |
|--------|------|
| User.php | 用户模块（注册、登录、编辑） |
| Index.php | 首页（动态列表） |
| Mood.php | 动态模块（发布、删除） |
| Chat.php | 聊天模块 |
| Friend.php | 好友模块（添加、删除） |
| Center.php | 个人中心 |
| Comment.php | 评论模块 |
| Collect.php | 收藏模块 |
| Favorite.php | 点赞模块 |

### 6.4 后台关键控制器

| 控制器 | 说明 |
|--------|------|
| Index.php | 后台首页 |
| User.php | 用户管理 |
| Mood.php | 动态管理 |
| Comment.php | 评论管理 |
| Sysinfo.php | 系统信息管理 |
| Login.php | 管理员登录 |

### 6.5 服务层（Service）

| 服务 | 说明 |
|------|------|
| User.php | 用户相关业务 |
| Mood.php | 动态相关业务 |
| Friend.php | 好友相关业务 |
| Comment.php | 评论相关业务 |
| Collect.php | 收藏相关业务 |
| Favorite.php | 点赞相关业务 |
| Sysinfo.php | 系统信息相关业务 |

---

## 七、部署与运行

### 7.1 环境要求

- PHP 5.4+
- MySQL 5.6+
- Web 服务器（Apache/Nginx）
- 可选：Workerman 运行环境（用于实时聊天）

### 7.2 部署步骤

1. 克隆项目代码到服务器
2. 配置 Web 服务器，将 DocumentRoot 指向 `public/` 目录
3. 导入数据库文件 `ch_chat.sql`
4. 修改数据库配置 `application/database.php`
5. 设置目录权限：`runtime/`、`Uploads/` 需要可写权限
6. 安装 Composer 依赖（如需要）
7. 启动 Socket.IO 服务：`php start_io.php start -d`
8. 访问系统

### 7.3 默认账号

**前台测试账号**：
- 用户名：user
- 密码：123456

**后台管理员账号**：
- 用户名：admin
- 密码：123456

---

## 八、安全注意事项

1. **密码加密**：用户密码使用 MD5 加密，建议升级为更安全的 bcrypt 或 password_hash
2. **SQL 注入防护**：使用框架提供的查询构造器，避免直接拼接 SQL
3. **XSS 防护**：对用户输入进行过滤和转义
4. **文件上传安全**：限制上传文件类型和大小
5. **会话安全**：设置合理的 Session 过期时间
6. **权限控制**：后台操作需要验证管理员权限

---

## 九、扩展与优化建议

1. **消息存储**：目前聊天消息仅在实时传递，建议增加消息存储功能
2. **消息推送**：增加离线消息推送功能
3. **图片上传**：动态和聊天支持图片上传
4. **表情支持**：聊天增加表情功能
5. **搜索功能**：增加用户搜索和动态搜索
6. **性能优化**：增加 Redis 缓存，优化数据库查询
7. **代码重构**：Service 层代码部分比较老旧，建议与 ThinkPHP 5.0 风格统一

---

## 十、总结

Vchat 是一个功能完整的社交聊天系统，涵盖了用户管理、好友关系、动态互动、实时聊天等核心社交功能。系统架构清晰，代码结构规范，具有良好的可扩展性和可维护性。

通过本业务文档，开发者可以快速了解系统的业务逻辑、数据库设计和核心功能实现，便于进行二次开发和维护。

